pipeline {
    agent {label 'Slave'}
    
    stages {
        stage('Test') {
        agent('Slave2') {label 'Slave'}
            steps {
                echo 'Testing'
            }
        }
        stage('Release') {
            steps {
                echo 'Releasing'
            }
        }
    }
}
